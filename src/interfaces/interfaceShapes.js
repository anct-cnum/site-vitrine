import PropTypes from 'prop-types';

const InterfaceShapes = {
  className: PropTypes.string,
  pictoUrl: PropTypes.string,
  altImg: PropTypes.string,
  title: PropTypes.node
};

export default InterfaceShapes;
